<?php
function connectDB() {
	## Database stuff
	global $db;
	$db = ($GLOBALS["___mysqli_ston"] = mysqli_connect('localhost', '@db1_user@', '@db1_pass@'));

	if (!$db) {
		die("Unable to connect to database");
	}

	if (!mysqli_select_db($GLOBALS["___mysqli_ston"], "@db1_name@")) 
	{		
		die("Unable to access Ready to Innovate database");
	}
}
